# FareSplit Backend (Express)

## Endpoints

- `GET /health`
- `POST /ai/ocr` (multipart/form-data, field: `image`) — stub
- `POST /ai/categorize` (json) — keyword categorization
- `POST /ai/parse-voice` (json) — stub extraction

## Local dev

```bash
npm install
npm run dev
```

